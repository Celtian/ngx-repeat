import { TestBed } from "@angular/core/testing";
import { NgxRepeatModule } from "./ngx-repeat.module";

describe('NgxRepeatModule', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      imports: [NgxRepeatModule]
    });
  });
})
